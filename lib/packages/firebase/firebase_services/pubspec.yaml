name: softi_firebase_services
description: A new Flutter package project.
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # softi_common:
  #   path: ../flutter_package_common

  # softi_common_auth:
  #   path: ../flutter_package_common_auth

  # softi_common_resource:
  #   path: ../flutter_package_common_resource

  # softi_common_remote_storage:
  #   path: ../flutter_package_common_remote_storage

  # google_sign_in: ^5.0.1
  # sign_in_with_apple: ^3.0.0
  # firebase_core: ^1.0.2
  # firebase_auth: ^3.3.11
  # firebase_dynamic_links: ^4.1.1
  # cloud_firestore: ^3.1.10
  # firebase_storage: ^10.2.9
  # firebase_database: ^9.0.8

# flutter packages pub run build_runner watch --delete-conflicting-outputs
# flutter pub run dependency_validator
dev_dependencies:
  flutter_test:
    sdk: flutter
  dependency_validator: ^3.2.0
  pedantic: ^1.11.0

flutter:

